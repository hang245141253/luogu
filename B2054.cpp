#include <iostream>
using namespace std;

int main() {
    int n, i;
    cin >> n;
    n = i;
    double sum = 0.0;
    int tmp;
    while (i--) {
        cin >> tmp;
        sum += tmp;
    }
    printf("%.2lf", sum / n);
    return 0;
}